export enum BorrowTransactionStatus {
  PENDING_PICKUP = 'pending_pickup',
  BORROWING = 'borrowing',
  RETURNED = 'returned',
  RETURN_LATE = 'return_late',
  REJECTED = 'rejected',
  CANCELED = 'cancelled',
  LOST = 'lost',
}
