export enum NotificationTypeEnum {
  BORROW = 'borrow',
  RETURN = 'return',
  PENALTY = 'penalty',
  VOUCHER = 'voucher',
  REWARD = 'reward',
  RANKING = 'ranking',
  ECO = 'eco',
  MANUAL = 'manual',
}
